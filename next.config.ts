import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Marketing site is the public-facing front for enhancedwellbeing.co
  // Portal lives at portal.enhancedwellbeing.co (separate Next.js app)
};

export default nextConfig;
